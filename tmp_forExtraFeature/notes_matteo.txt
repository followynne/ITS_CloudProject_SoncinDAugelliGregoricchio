- controllare che una persona sia loggata altrimenti "redirect page 404"

- inserire il container corretto della persona ($_SESSION)

- .env: va tolta da git tramite git ignore, spostata in root altra x farla rimuovere, usare 2*key per modificare la conn string rispetto a quelle salvata in vecchi commit

- aggiungere tutte le operazioni di rapport ocn il Database!

- link condivisibile per le foto button
azure storage select blob and make them shareable via token


- mostrare dettagli exif nella pagina della foto singola (cfr DB request)


- in ottica ricerca tag, pensare a implementare ricerca tramite tag (che tag?!).
