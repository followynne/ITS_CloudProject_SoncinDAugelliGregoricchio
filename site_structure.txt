
start -> if already logged => redirect to index
      -> if signup clicked redirect to signup
      -> if login tried but 401/403: -> _login -> home

signup -> register -> home

index -> homepage -> home

getblobsperpage -> _completegallery -> gallery

getallblobs-> _completegallery -> gallery

showsingleblob-> _imagedetail -> gallery
